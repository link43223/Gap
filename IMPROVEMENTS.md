# Gap 改进清单

> 来源：资深英语阅读体验师审查（2026-08-02）+ 代码审查
> 状态说明：`[ ]` 待做 · `[x]` 完成 · 每完成一项就在这里勾选并注明日期

---

## ✅ 已完成

- [x] TTS 底部悬浮播放控制条（淡入淡出、查词时自动隐藏、图标化播放按钮）
- [x] 机器音 bug 修复（退出文章后系统语音不再停不下来）
- [x] 控制条固定在屏幕底部（不受文章 transform 影响）
- [x] 收藏功能彻底移除

---

## 🔴 紧急 bug（必修，影响所有用户）

- [x] **缩写词点词查义错误**：`don't` 被切成 `don` + `'t`，`it's`/`can't`/`won't`/`I'll` 全部中招。
  已修复（2026-08-02）：`makeWordsClickable` 分词正则支持 `词'词` 缩写，`don't`/`it's`/`can't`/`shouldn't` 等作为整体词查义。
- [x] **句子切分不可靠**：`e.g.`/`i.e.`/`Ph.D.`/`3.14`/省略号 `...` 会错误断句。
  已修复（2026-08-02）：`isAbbrevAt` 增加"点前后都是字母/数字 → 不视为句末"规则，并补充多点缩写白名单，`e.g.`/`i.e.`/`Ph.D.`/`U.S.`/小数均正确。

---

## 🟠 核心升级（从"阅读器"变"学习工具"）

- [ ] **单词库复习功能**：闪卡 + 记忆曲线（Leitner/SM-2），"今日待复习"入口。
  影响：没有复习 = 24 小时忘 70%，单词库沦为收藏夹。
- [ ] **读后理解检测**：每篇文章读完弹出 2-3 道选择题 + 本文查过的词回顾。
- [ ] **查过的词保留视觉标记**：查词后词在文中淡色高亮（本次会话内），回看找得到。
- [ ] **阅读进度记忆**：记录 `{文章, 滚动位置}`，长文续读 + 列表显示进度。

---

## 🟡 内容与体验（提升质量）

- [ ] **文章体裁多样化**：现在全是科普说明文，补充故事/对话/新闻等文体。
- [ ] **难度分级校正**：`science-9` 标难度1却含学术词，用可读性算法重标（Flesch-Kincaid 等）。
- [ ] **词典加例句/搭配**：CET4/6 高频词补 1-2 句例句。
- [ ] **新手引导**：首次进入自评水平 + 推荐起始文章。

---

## 🟢 增强（差异化亮点）

- [ ] **TTS 逐词高亮**：edge-tts `--boundary` 输出词级时间戳，做词级跟读高亮（相比扇贝/薄荷的独特优势）。
- [ ] **沉浸阅读模式**：隐藏顶部栏只留正文。
- [ ] **语速扩展**：0.8/1/1.2x → 0.5x-1.5x 步进 0.1x。
- [ ] **阅读统计/打卡**：读了几天、多少篇、累计词汇。
- [ ] **英美音标变体**：音标数据区分英式/美式。

---

## 📦 代码审查遗留（发布前参考）

- [ ] 「每日精选」空页面：articles.js 无 daily 文章，填充或隐藏该标签页。
- [ ] SEO 标签：补 `<meta name="description">` + Open Graph + favicon。
- [ ] 首屏白屏：articles.js 238KB 同步阻塞，改按话题懒加载。
- [ ] iPhone 刘海屏：查词弹窗加 `safe-area-inset-bottom`。
- [ ] 长文章渲染：`makeWordsClickable` 逐词绑定事件 → 改事件委托。

---

## 🎯 建议推进顺序

```
第 1 步：修两个 bug（缩写词 + 句子切分）→ 小改动，做完推上线
第 2 步：单词库闪卡复习 → 学习闭环的钥匙
第 3 步：读后检测 + 查词标记 + 阅读进度 → 补完整闭环
第 4 步：TTS 逐词高亮 → 打出差异化
```
